// const fetchPosts = async (delay = 1000) => {
//   return new Promise((resolve) => {
//     setTimeout(() => resolve(posts), delay);
//   });
// };

// const fetchPostBySlug = async (slug, delay = 1000) => {
//   return new Promise((resolve) => {
//     setTimeout(() => {
//       const post = posts.find((p) => p.slug === slug);
//       resolve(post);
//     }, delay);
//   });
// };

// export { fetchPosts, fetchPostBySlug };
